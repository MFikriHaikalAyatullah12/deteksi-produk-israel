import './globals.css'
import type { Metadata } from 'next'
import { Inter } from 'next/font/google'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Deteksi Produk Israel - AI Detection System',
  description: 'Sistem deteksi produk terafiliasi Israel menggunakan teknologi AI Random Forest',
  keywords: 'deteksi, produk, israel, AI, computer vision, random forest',
  authors: [{ name: 'Deteksi Produk Israel Team' }],
  viewport: 'width=device-width, initial-scale=1',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="id">
      <body className={inter.className}>
        <div className="min-h-screen bg-gradient-to-br from-blue-50 to-indigo-100">
          {children}
        </div>
      </body>
    </html>
  )
}